export { default as modalReducer } from './slice';
export { openModal, closeModal, setModalData } from './slice';
export type { ModalType, ModalState, OpenModalPayload } from './types';
export { useModal } from './hooks';
